{
  "name": "luxly",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "dotenv": "^17.2.3",
    "zod": "^4.1.13"
  },
  "scripts": {
    "postinstall": "cd packages/prisma && bunx --bun prisma generate",
    "copyenv": "cp .env.example .env ./apps/api/.env ./apps/web/.env ."
  }
}
